# Stroke SLP Archived Scripts

These scripts are kept for reference only. They are not run by
`run_pipeline.py` or `run_analysis_outputs.py`.

- `exploratory/` contains useful one-off checks and design explorations.
- `legacy/` contains stale scripts that use older timing groups or output
  conventions.

Use `../ANALYSIS_INDEX.md` for the current canonical script map.
